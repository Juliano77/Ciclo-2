def is_prime(x):
    if x < 2:
        return Falso
    else:
        for n in range(2,x):
            if x % n == 0:
               return Falso
        return Verdadeiro

        for x in range(1, 101):
            if is_prime(x)
